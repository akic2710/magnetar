# QA-only build outputs, regenerated by `node build.mjs`.
# dist/index.html IS tracked - it is the file uploaded to CrazyGames.
dist/test.html
dist/.check.js

# Frame scratch for the README GIF, rebuilt on demand.
.gifwork/
