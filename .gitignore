# QA-only build outputs, regenerated by `node build.mjs`.
# dist/index.html IS tracked - it is the file uploaded to CrazyGames.
dist/test.html
dist/.check.js

# One-shot patch scripts used to transform src/ during development. Each was
# applied once and asserts against text that no longer exists, so re-running
# them fails by design. src/ is the source of truth.
scripts/
